name = "amg-email-worker"
main = "worker.js"
compatibility_date = "2024-01-01"

# Deploy:
#   cd infra/cloudflare-email-worker
#   npx wrangler deploy
#
# Després d'aquest deploy, el Worker apareixerà a:
#   Workers & Pages → amg-email-worker → Settings → Triggers → Add trigger → Email
# I posa el patró: *@inbound.amgdl.com
