# URL del backend (en local, el proxy de next.config.js redirigeix /api/* al backend)
NEXT_PUBLIC_API_URL=http://localhost:8080

# URL pública del frontend (usada per canonical i OG tags)
NEXT_PUBLIC_SITE_URL=https://amg.digital

# Entorn (development | production)
NODE_ENV=development
